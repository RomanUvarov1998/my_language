// Guessing game:
var a: f32 = 3; // 0..7

if a > 3 { // 4..7
	if a > 5 { // 6..7
		if a > 7 { @print(8); } else { @print(7); }
	} else { // 4..5
		if a > 4 { @print(5); } else { @print(4); }
	}
} else { // 0..3
	if a > 1 { // 2..3
		if a > 2 { @print(3); } else { @print(2); }
	} else { // 0..1
		if a > 0 { @print(1); } else { @print(0); }
	}
}
